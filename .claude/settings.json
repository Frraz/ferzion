{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath // empty' | { read -r f; case \"$f\" in */backend/*.py) \"/home/ferraz/Área de trabalho/Projetos/Ferzion/backend/.venv/bin/ruff\" format \"$f\" ;; esac; } 2>/dev/null || true",
            "statusMessage": "Formatando Python com ruff..."
          }
        ]
      }
    ]
  }
}